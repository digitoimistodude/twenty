# Copy to .env on the server (NOT committed). Fill real values.
RELAY_PORT=8787
# openssl rand -hex 32  (also set as the Twenty webhook's secret)
TWENTY_WEBHOOK_SECRET=
# Slack incoming webhook for the target channel (#myynti)
SLACK_WEBHOOK_URL=
TWENTY_BASE_URL=https://crm.dude.fi
# Twenty API key (used to resolve related record names for the messages)
TWENTY_API_TOKEN=
